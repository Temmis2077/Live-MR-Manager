# 기여하기

OSW는 노래하는 스트리머를 위한 로컬 구동형 스템 분리 도구입니다. 작게 시작해
개방형 미니 DAW로 가는 중이라, 실제로 방송·연습에 쓰면서 나온 제보가 가장
도움이 됩니다.

## 어떤 기여가 특히 반가운가

- **실사용 버그 제보** — 특히 가사 정렬이 어긋나는 곡. 이 프로젝트에서 가장
  중요한 로직이고, 어긋난 실제 사례가 있어야 고칠 수 있습니다.
- **분리 모델 프리셋** — 잘 되는 커스텀 모델 조합 (`docs/CUSTOM_MODELS.md`)
- **다른 GPU에서의 성능 수치** — README의 실측표를 채우는 데 씁니다.
- 번역, 문서, 오탈자

## 개발 환경

```bash
npm install
npm run tauri dev
```

사전 요구는 [README의 개발 환경](README.md#-개발-환경) 절을 보세요.

## 바꾸기 전에

- **큰 변경은 먼저 이슈로 이야기해 주세요.** 방향이 안 맞으면 서로 시간이
  아깝습니다.
- UI를 건드린다면 [docs/UI_DESIGN_GUIDELINES.md](docs/UI_DESIGN_GUIDELINES.md)를
  먼저 읽어 주세요. 색·간격·모션·접근성 기준이 정해져 있습니다.

## 테스트

```bash
npm test              # 프런트엔드 (vitest)
cargo test --lib      # src-tauri 안에서 실행
```

로직을 바꿨다면 테스트를 같이 주세요. 특히 가사 파싱·정렬·큐는 회귀가 잘
생기는 곳이라 테스트가 촘촘합니다.

## 코드 스타일

정해진 린터는 없습니다. **주변 코드와 같게** 써 주세요.

- 주석은 한국어. 무엇을 하는지가 아니라 **왜 그렇게 했는지**를 적습니다.
  (특히 우회한 이유, 안 되는 방법을 피한 이유)
- 프런트엔드는 프레임워크 없는 바닐라 JS + ES 모듈입니다.
- 죽은 버튼을 넣지 마세요. 기능 없는 UI는 릴리즈에 나가지 않습니다.

## 커밋 메시지

```
feat(live): 출력 장치를 라이브에서 바로 바꾸는 드롭다운

왜 그렇게 했는지, 무엇을 확인했는지.
```

`feat` / `fix` / `refactor` / `docs` / `test` / `chore` + 범위. 본문은 한국어로
편하게 쓰되, **무엇을 확인했는지**를 남겨 주세요.

## 라이선스

기여한 코드는 프로젝트와 같은 [MIT 라이선스](LICENSE)로 배포됩니다.

서드파티 코드·에셋·모델을 추가한다면 [docs/THIRD-PARTY.md](docs/THIRD-PARTY.md)에
출처와 라이선스를 함께 적어 주세요. 라이선스를 확인할 수 없는 것은 넣지 않습니다.
