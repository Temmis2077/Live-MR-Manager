# OSW 변경 기록

## `1.0.0-beta.1` — 2026-09-03

소수 외부 테스터용 첫 OSW 독립 베타입니다. 자동 테스트 통과와 실제 장치·배포
검증은 구분해서 기록합니다.

### 제품 독립

- 제품명·앱 식별자·저장소·업데이트 경로를 OSW로 분리
- OSW 전용 사용자 데이터 경로(이전 제품 데이터는 가져오거나 이동하지 않음)
- `osw://` 딥링크를 기본으로 사용하고 이전 스킴은 입력 호환용으로 유지
- 기존 GPU 팩·모델을 재사용하는 관리형 도구 공유 캐시 유지

### 곡 추가와 라이브러리

- 언어권별 가사 검색어·출처 우선순위 조정
- LRCLIB 싱크 가사 가져오기와 곡 정보 자동 채우기
- 라이브러리 필터·패널·곡 추가 화면 재구성
- MR 캐시 경로와 WAV/MP3 저장 형식 선택
- 설정까지 포함하는 백업·복원

### MR 분리와 모델

- 기본 분리 모델을 MIT 라이선스의 `smank/mel-band-roformer-vocals-onnx`로 교체
- 기존 Kim/Inst/Deux 계열은 기본·자동 다운로드에서 제외하고 커스텀 모델로 분리
- 기본 모델 다운로드를 고정 리비전으로 제한하고 SHA-256 무결성 검사 추가
- 기존 기본 모델 설정은 MIT 모델로 자동 마이그레이션하며 로컬 커스텀 파일은 보존
- 선택 설치형 TensorRT GPU 팩과 런타임 로딩 경로 정리
- 커스텀 ONNX 모델 등록·검증 강화
- 리드/백킹 보컬을 위한 실험적 karaoke 모델 경로
- 부분 파일·백업 파일을 이용한 결과 교체 안전성 개선
- 설치본에 ONNX Runtime 1.24와 CUDA·TensorRT provider를 포함하고 전체 경로로 고정 로드

### AI 가사 정렬

- 한국어·영어·혼합곡 CTC 정렬과 전역 품질 필터 개선
- 앵커·신뢰도·반복 가사·길이 타당성 처리 강화
- 보컬 활동 구간 저장, 파형 음영, 경계 스냅
- 가사 블럭 경계 선택과 Enter/Shift+Enter 재배치
- 정렬 메타데이터와 입력 출처를 저장해 오래된 결과를 구분
- 정렬 로그 분석 도구와 karaoke ONNX 변환·검증 도구 추가

### 라이브 화면과 오버레이

- 현재/다음 가사와 줄 내부 가라오케 진행도
- 라이브 화면과 OBS 오버레이의 재생 시계 통일
- 가사 전환 지연·깜빡임·편집 후 표시 회귀 수정
- 곡 정보/가사별 표시 항목, 외곽선, 그림자, 그라디언트, 진행바, 프리셋
- 오버레이 타이밍 보정과 개발용 미리보기
- 라이브 수행 상태와 재생 UI 동기화

### 사용 흐름과 UI

- 첫 실행 온보딩과 앱 모드 선택
- 단축키 체계와 도움말
- 화면 이동 기록, 설정 탭, 모달 레이어 관리
- 공개 브랜드 에셋과 UI 토큰 재정리

### 현재 자동 검증에서 확인한 것

- `npm.cmd test`: 41개 테스트 파일, 491개 테스트 통과
- `cargo test --lib`: 113개 테스트 통과
- `npm.cmd run build`: 릴리즈 계약·IPC 경계·TypeScript 검사와 Vite 프로덕션 빌드 통과
- 자동 검증은 실제 Tauri 창, 오디오 장치, OBS, 장시간 재생 또는 NSIS 설치본 검증을 뜻하지 않음

### 설치 후보에서 실제 확인한 것

- Windows NSIS 후보의 상대 자산 경로를 수정하고 재설치해 UI 표시를 확인함
- 기존 설치 위 NSIS 덮어설치, 실행, 종료와 재실행
- 설치 폴더의 ONNX Runtime 1.24와 provider 로드
- 현재 Windows PC에서 TensorRT MR 분리 1곡 완료(약 51초), 가사 정렬·LRC 저장·MR 재생

### 알려진 제한과 후속 검증

- 앱 내 자동 설치가 아니라 알림에서 GitHub 릴리즈 페이지를 열어 수동 설치
- 현재 장비 외 오디오 장치와 OBS 연결 조합
- 60분 연속 재생과 3회 이상 곡 전환
- 특수문자·한글·다른 드라이브 음원 경로와 MR 캐시 경로 변경
- GPU별 TensorRT 설치와 폴백(검증하지 못한 조합은 알려진 제한으로 공개)
- 깨끗한 별도 PC 검증은 소수 테스터 배포 후 공개 범위를 넓히기 전 필수 관문

이전 `v0.6.0-beta.1~6`은 공개 릴리스에서 제거했으며 기록은
[0.6 Legacy 기록](docs/RELEASE_HISTORY_0.6.md)에 보존합니다.
