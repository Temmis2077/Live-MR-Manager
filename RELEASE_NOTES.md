# OSW 릴리즈 노트

## v1.0.0-beta.1 — Open Stem Wave 독립 릴리즈

**OSW의 첫 독립 릴리즈입니다.** 이름·로고·브랜드를 새로
짓고, 개방형 스템 분리 미니 DAW라는 방향을 명확히 했습니다.

### ⚠️ 처음 실행 시 — 이전 데이터 가져오기
OSW는 이전 버전과 **별도 앱으로 설치**됩니다(내부 식별자 변경). 처음 실행하면
창이 뜨기 전에 **"이전 버전(Live MR Manager)의 데이터를 가져올까요?"** 를 묻습니다.
- **예**: 라이브러리·MR 분리 결과·다운로드한 AI 모델을 OSW로 옮깁니다(같은 드라이브라 즉시 완료).
- **아니오**: 빈 상태로 시작합니다.
- 관리형 도구(ffmpeg·yt-dlp·GPU 가속 팩)는 그대로 재사용되어 다시 받지 않습니다.

### 🌊 리브랜딩
- **OSW (Open Stem Wave)** — Open(개방) · Stem(스템 분리) · Wave(WAV·파형). 로고는 보라 토성 마크 + 그리스 문자 워드마크 `Ο.Σ.Ω`(Ω=옴/오디오, Σ=합/스템 합성).
- 앱 전체(사이드바·오버레이·가사창)를 브랜드 보라로 통일.

### 🎛️ 라이브 / 녹음 모드
- 첫 실행 때 용도를 고릅니다(설정에서 언제든 전환).
- **라이브(방송·공연)**: 실시간 MR·모니터/MR 채널·OBS 오버레이. 넉넉한 라운드 톤.
- **녹음(커버 제작)**: 스템 분리·믹싱 중심의 미니 DAW 방향. 각지고 절제된 콘솔 톤. (악기 분리·전용 화면은 준비 중)

### 🤖 가사 정렬 강화
- **앵커 제약 정렬** — 수동으로 맞춘 줄·보컬시작 마커를 하드 앵커로 삼아, 한 번의 밀림이 곡 전체로 전파되지 않게. 고친 줄이 다음 정렬의 기준이 되어 곡마다 정확해집니다.
- **줄별 신뢰도 표시** — 음향 확신도가 낮은 줄에 "확인" 배지를 붙여 우선 검토를 유도.

### ⚡ GPU 가속 팩 앱 내 자동 다운로드
- NVIDIA GPU에서 MR 분리를 곡당 ~30초로. 설정에서 **다운로드 버튼 한 번**으로 받아 설치(분할 다운로드·검증·자동 설치).

### 🗂️ 곡 관리 개선
- 곡 정보 일괄 편집을 라이브러리 상단 **곡 관리** 버튼으로 이동, **카테고리·장르·가사상태별 필터** 추가.
- 설정 화면을 하위 탭(일반·미디어·AI 엔진·라이브러리·정보)으로 분류해 정리.

### 🔧 안정성
- **yt-dlp 자동 최신화** — 오래되면 시작 시 갱신, 추출 실패 시 즉시 갱신 후 재시도. "어느 날 갑자기 곡 추가가 안 됨"을 예방.
- **ffmpeg 다운로드 폴백** — 소스 하나가 죽어도 대체 소스로 설치.
- 곡 추가·정렬 시 정렬 모델이 없으면 다운로드를 유도.

---

기반: [AutumnColor77/Live-MR-Manager](https://github.com/AutumnColor77/Live-MR-Manager) — 초기 오디오 엔진·AI 분리·라이브러리·OBS 오버레이의 설계·구현 저작권은 원 개발자에게 있습니다.
