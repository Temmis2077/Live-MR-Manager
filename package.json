{
  "name": "osw",
  "private": true,
  "version": "1.0.0-beta.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "node scripts/dev-server.mjs --root src --port 1420",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vitest": "^3.2.4"
  }
}
