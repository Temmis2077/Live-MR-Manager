// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE } from "@tauri-apps/api/core";
import * as __TAURI_EVENT from "@tauri-apps/api/event";

/** Commands */
export const commands = {
	// 사용 가능한 출력 장치 목록. `isActive`는 현재 열려 있는 장치.
	listOutputDevices: () => typedError<OutputDevice[], ApiError>(__TAURI_INVOKE("list_output_devices")),
	// 현재 선택된 출력 장치 이름(빈 문자열이면 시스템 기본).
	getOutputDevice: () => __TAURI_INVOKE<string>("get_output_device"),
	/**
	 *  출력 장치를 실시간 전환한다. 재생 중이면 현재 위치에서 새 장치로 이어서
	 *  재생하고, 마스터/보컬/MR 볼륨·피치·템포는 공유 상태라 그대로 유지된다.
	 *  `name`이 빈 문자열이면 시스템 기본 장치로 되돌린다.
	 */
	setOutputDevice: (name: string) => typedError<string, ApiError>(__TAURI_INVOKE("set_output_device", { name })),
	// 현재 MR 채널(2번째 출력) 장치 이름. 빈 문자열이면 MR 채널 꺼짐.
	getMrOutputDevice: () => __TAURI_INVOKE<string>("get_mr_output_device"),
	playTrack: (path: string, durationMs: number | null, playNow: boolean | null) => typedError<number, ApiError>(__TAURI_INVOKE("play_track", { path, durationMs, playNow })),
	togglePlayback: () => typedError<boolean, ApiError>(__TAURI_INVOKE("toggle_playback")),
	stopPlayback: () => typedError<null, ApiError>(__TAURI_INVOKE("stop_playback")),
	seekTo: (positionMs: number) => typedError<null, ApiError>(__TAURI_INVOKE("seek_to", { positionMs })),
	getPlaybackState: () => typedError<AppState, ApiError>(__TAURI_INVOKE("get_playback_state")),
	/**
	 *  MR 채널(2번째 출력) 장치를 설정한다. 빈 문자열이면 MR 채널을 끈다(모니터만).
	 *  곡이 로드돼 있으면 현재 위치에서 두 채널을 다시 구성한다.
	 */
	setMrOutputDevice: (name: string) => typedError<string, ApiError>(__TAURI_INVOKE("set_mr_output_device", { name })),
	getMixState: () => typedError<MixState, ApiError>(__TAURI_INVOKE("get_mix_state")),
	// 트랙별 독립 페이더(0~100%). track: "vocal" | "inst".
	setTrackFader: (track: string, percent: number) => typedError<null, ApiError>(__TAURI_INVOKE("set_track_fader", { track, percent })),
	// 트랙 음소거. track: "vocal" | "inst".
	setTrackMute: (track: string, muted: boolean) => typedError<null, ApiError>(__TAURI_INVOKE("set_track_mute", { track, muted })),
	// 트랙 솔로. track: "vocal" | "inst". 솔로가 켜진 트랙만 소리난다.
	setTrackSolo: (track: string, soloed: boolean) => typedError<null, ApiError>(__TAURI_INVOKE("set_track_solo", { track, soloed })),
	// 채널 라우팅 설정. "vocal"은 리드 보컬의 호환 별칭이다.
	setChannelRoute: (channel: string, source: string, enabled: boolean) => typedError<null, ApiError>(__TAURI_INVOKE("set_channel_route", { channel, source, enabled })),
	/**
	 *  메트로놈 설정(활성/BPM/게인). bpm 0 = 곡 분석 BPM 자동(미구현 시 120).
	 *  bpm 변경은 다음 재생/탐색부터 반영된다(생성된 클릭 스트림 특성상).
	 */
	setMetronome: (enabled: boolean, bpm: number, gain: number) => typedError<null, ApiError>(__TAURI_INVOKE("set_metronome", { enabled, bpm, gain })),
	/**
	 *  버스 지연 보정(ms) 설정. bus: "monitor" | "mr". 값은 ≥0. 딜레이는 재생
	 *  파이프라인에 붙으므로, 곡이 로드돼 있으면 현재 위치에서 재구성해 즉시 반영한다.
	 */
	setBusDelay: (bus: string, delayMs: number) => typedError<null, ApiError>(__TAURI_INVOKE("set_bus_delay", { bus, delayMs })),
	// 출력 소프트 리미터 on/off. 재생 중 즉시 반영(리미터 소스가 atomic을 읽음).
	setLimiter: (enabled: boolean) => typedError<null, ApiError>(__TAURI_INVOKE("set_limiter", { enabled })),
	loadLibrary: () => typedError<SongMetadata[], ApiError>(__TAURI_INVOKE("load_library")),
	saveLibrary: (songs: SongMetadata[]) => typedError<null, ApiError>(__TAURI_INVOKE("save_library", { songs })),
	updateSongMetadata: (song: SongMetadata) => typedError<null, ApiError>(__TAURI_INVOKE("update_song_metadata", { song })),
	deleteSong: (path: string) => typedError<null, ApiError>(__TAURI_INVOKE("delete_song", { path })),
	getGenres: () => typedError<Genre[], ApiError>(__TAURI_INVOKE("get_genres")),
	getCategories: () => typedError<Category[], ApiError>(__TAURI_INVOKE("get_categories")),
	pruneUnusedTaxonomy: () => typedError<[number, number], ApiError>(__TAURI_INVOKE("prune_unused_taxonomy")),
};

/** Events */
export const events = {
	playbackProgress: makeEvent<PlaybackProgress>("playback-progress"),
	playbackStatus: makeEvent<PlaybackStatus>("playback-status"),
};

/* Types */
// Stable error contract crossing the Rust -> frontend boundary.
export type ApiError = {
	code: string,
	message: string,
	recoverable: boolean,
	details: { [key in string]: string } | null,
};

export type AppState = {
	currentTrack: string | null,
	pitch: number,
	tempo: number,
	volume: number,
	vocalBalance: number,
	vocalEnabled: boolean,
	lyricEnabled: boolean,
	isPlaying: boolean,
	vocalFader: number,
	backingFader: number,
	instFader: number,
	vocalMuted: boolean,
	backingMuted: boolean,
	instMuted: boolean,
	vocalSolo: boolean,
	backingSolo: boolean,
	instSolo: boolean,
	monRouteVocal: boolean,
	monRouteBacking: boolean,
	monRouteInst: boolean,
	monRouteMetro: boolean,
	mrRouteVocal: boolean,
	mrRouteBacking: boolean,
	mrRouteInst: boolean,
	mrRouteMetro: boolean,
	metroEnabled: boolean,
	metroBpm: number,
	metroGain: number,
};

export type Category = {
	id: number,
	name: string,
};

export type Genre = {
	id: number,
	name: string,
};

// 믹스·라우팅·메트로놈 상태(UI 복원용).
export type MixState = {
	vocalFader: number,
	backingFader: number,
	instFader: number,
	vocalMuted: boolean,
	backingMuted: boolean,
	instMuted: boolean,
	vocalSolo: boolean,
	backingSolo: boolean,
	instSolo: boolean,
	monRouteVocal: boolean,
	monRouteBacking: boolean,
	monRouteInst: boolean,
	monRouteMetro: boolean,
	mrRouteVocal: boolean,
	mrRouteBacking: boolean,
	mrRouteInst: boolean,
	mrRouteMetro: boolean,
	metroEnabled: boolean,
	metroBpm: number,
	metroGain: number,
	mrDevice: string,
	monDelayMs: number,
	monEstLatencyMs: number,
	mrDelayMs: number,
	mrEstLatencyMs: number,
	limiterEnabled: boolean,
	vocalBalance: number,
	vocalEnabled: boolean,
	masterVolume: number,
	pitch: number,
	tempo: number,
	backingAvailable: boolean,
};

export type OutputDevice = {
	name: string,
	config: string,
	isActive: boolean,
};

export type PlaybackProgress = {
	positionMs: number,
	durationMs: number,
};

export type PlaybackStatus = {
	status: Status,
	message: string,
};

export type SongMetadata = {
	id: number | null,
	title: string,
	thumbnail: string,
	duration: string,
	source: string,
	path: string,
	pitch: number | null,
	tempo: number | null,
	volume: number | null,
	artist: string | null,
	tags: string[] | null,
	genre: string | null,
	categories: string[] | null,
	playCount: number | null,
	dateAdded: number | null,
	isMr: boolean | null,
	isSeparated: boolean | null,
	hasLyrics: boolean | null,
	/**
	 *  가사 싱크 상태: "synced"(타임스탬프 있음) / "unsynced"(가사만) / "none"(가사 없음).
	 *  `get_songs_internal`에서만 채워지고, 다른 생성 경로는 None.
	 */
	lyricSyncStatus?: string | null,
	originalTitle: string | null,
	translatedTitle: string | null,
	curationCategory: string | null,
	songKey?: string | null,
	bpm: number | null,
	difficulty: number | null,
	proficiency: number | null,
	karaokeUrl: string | null,
	coverUrl: string | null,
	originalUrl: string | null,
	lyricsLink: string | null,
	melomingSongId: number | null,
	melomingChannelId: number | null,
	melomingArtistId: number | null,
	// 멜로밍 API `categoryIds` (1–N개)
	melomingCategoryIds: number[] | null,
	syncStatus: string | null,
};

export type Status = "pending" | "downloading" | "decoding" | "separating" | "playing" | "paused" | "error" | "finished";

/* Tauri Specta runtime */
async function typedError<T, E>(result: Promise<T>): Promise<{ status: "ok"; data: T } | { status: "error"; error: E }> {
    try {
        return { status: "ok", data: await result };
    } catch (e) {
        if (e instanceof Error) throw e;
        return { status: "error", error: e as any };
    }
}

function makeEvent<T>(name: string) {
    const base = {
        listen: (cb: __TAURI_EVENT.EventCallback<T>) => __TAURI_EVENT.listen(name, cb),
        once: (cb: __TAURI_EVENT.EventCallback<T>) => __TAURI_EVENT.once(name, cb),
        emit: ((payload: T) => __TAURI_EVENT.emit(name, payload) as unknown) as (T extends null ? () => Promise<void> : (payload: T) => Promise<void>)
    };

    const fn = (target: import("@tauri-apps/api/webview").Webview | import("@tauri-apps/api/window").Window) => ({
        listen: (cb: __TAURI_EVENT.EventCallback<T>) => target.listen(name, cb),
        once: (cb: __TAURI_EVENT.EventCallback<T>) => target.once(name, cb),
        emit: ((payload: T) => target.emit(name, payload) as unknown) as (T extends null ? () => Promise<void> : (payload: T) => Promise<void>)
    });

    return Object.assign(fn, base);
}

